/*As every language, Rust possesses a main function. It is always the first code that runs in every executable Rust program. */

/*println! calls a Rust macro. (!) indicates that is a macro rather than a function call.*/

fn main() {
    println!("Hello, world!");
}